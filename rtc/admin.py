# Presence is tracked in-process by the SSE layer (rtc/sse.py), not in the
# database, so there are no models to register here. (Previously this file
# registered channels_presence's Room/Presence models.)
